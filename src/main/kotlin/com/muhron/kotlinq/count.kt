package com.muhron.kotlinq

// count() and <T> count(predicate : (T) -> Boolean) methods are implemented in kotlin.collections or kotlin.sequences
// The methods are same name and same parameter name.
